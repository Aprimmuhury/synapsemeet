/**
 * SynapseMeet - config.js
 * Sets the API base URL explicitly. MUST load before api.js on every page.
 *
 * This is what makes the app work independently of any local network —
 * WiFi, emulator host addresses (10.0.2.2), etc. are no longer needed.
 *
 * >>> EDIT THIS after you deploy the backend (see DEPLOYMENT.md) <<<
 * Replace the URL below with your real backend URL + "/api".
 */
window.SYNAPSEMEET_API_BASE = 'https://synapsemeet.onrender.com/api';
